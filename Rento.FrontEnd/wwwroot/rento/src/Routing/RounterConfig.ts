import { Routes } from '@angular/router';
import { TripComponent } from '../Pages/Trip/trip.component';

  export class AppRountes {
    static Pages = [
      { path: 'trip', component: TripComponent }
    ]
  }